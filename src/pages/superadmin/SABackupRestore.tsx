import ComingSoonPage from '@/components/common/ComingSoonPage';
import { Database } from 'lucide-react';
export default function SABackupRestore() {
  return <ComingSoonPage title="Backup & Restore" icon={Database} />;
}
